# Optional. The complete game works with the authored field guide without a key.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=anthropic/claude-opus-5
PORT=5174
# Bind publicly only when deliberately deploying behind your hosting proxy.
HOST=127.0.0.1
